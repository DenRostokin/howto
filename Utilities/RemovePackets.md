Для удаления пакетов со всеми зависимостями используется:

$ apt-get remove --purge <file_name>

Также можно удалить пакеты по выражению. Например:

$ apt-get remove --purge '*impacket*'

В данном случае кавычки обязательны!!!
